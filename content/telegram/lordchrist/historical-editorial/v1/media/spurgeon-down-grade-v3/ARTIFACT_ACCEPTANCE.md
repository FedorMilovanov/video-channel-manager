# Artifact acceptance

The only accepted Spurgeon v3 editorial media byte identities are the SHA-256/byte-length/dimension/MIME records already sealed in `../../v3/spurgeon-down-grade-1887/revision.json`.

This artifact lane must materialize exactly those bytes. It does not change `transport_ready=false`, does not create a provider release, and authorizes zero Telegram writes.
