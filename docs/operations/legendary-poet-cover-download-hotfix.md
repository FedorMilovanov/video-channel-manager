# Legendary Poet cover download hotfix

The approved article policy remains immutable. The preparation layer may map the exact reviewed Wikimedia Commons `Special:Redirect` URL for `Esenin1925.jpg` to the corresponding stable `upload.wikimedia.org` original-file URL solely for downloading bytes.

This changes no VK post text, article URL, scheduled timestamp, project binding, policy digest, or cover identity.
