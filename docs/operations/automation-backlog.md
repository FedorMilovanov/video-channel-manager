# Operational automation backlog

Updated: 2026-08-05  
Program state: `WAVES_0_12A_ENGINEERING_GOVERNANCE_COMPLETED_LIVE_RECONCILIATION_PENDING_NO_PROVIDER_WRITES`

This backlog is subordinate to [`current-state.md`](current-state.md), the canonical audit, the v3 machine-state overlay, and its immutable v2 predecessor register.

## Completed program foundation

- Waves 0–7: exact identity, durable mutation journals, upload/wall separation, guarded operator, versioned Wave Engine, and fault/corruption/replay proofs.
- Audit A0: authoritative roadmap/finding ownership synchronized.
- Waves 8A–8F: exact-first matching, canonical/catalog/media/thumbnail correctness, and integration evidence.
- Wave 9 read-only contract: immutable bounded reconciliation input and classifications.
- Package A / Waves 9A–9B–10 tooling: SHA-bound ingest, recovery ledger, read-only operator board, runbook, rollback, retirement, and archive governance.
- Wave 11: operational-package truth levels, acceptance verifier, `filter=moder` regression, source-bound incident archive, and retired package family.
- Wave 12: deterministic Windows handoffs and post-Wave-11 roadmap convergence.
- Wave 12A / #118: project-bound issue ownership corrected and machine-tested. PR #120 merged as `30c1ec11040034f6d3ed2492afe1bc7c029db1d0`; exact-head CI `30971070928` passed `785 passed, 1 xfailed` on Python 3.11/3.12/3.13 and all three PowerShell environments. Provider queries, provider writes, and write plans: `0`.

## Active read-only operational work

### #31 — Lord God long-form reconciliation

Exact binding: `lord-god-strength`, OAuth alias `fedor-milovanov`, YouTube `UCeSJsC6go2c9pdJCuUI1BYA`, VK community/owner `60805374` / `-60805374`.

Required:

1. exact `upload-result.json`, `upload-ledger.db`, latest exact log, and bounded source manifest;
2. fresh bounded read-only YouTube/VK snapshots;
3. Package A evidence, recovery ledger, and operator board;
4. exact per-item states and no blind replay.

Do not rerun the old 26-item launcher or sermon-month v1/v2/v3.

### #32 — Lord God Shorts/Clips reconciliation

Exact binding: `lord-god-strength`, alias `fedor-milovanov`, community/owner `60805374` / `-60805374`.

Required: exact bounded source set; all local plan/result/journal/SQLite evidence; fresh ordinary-video and Clip-surface snapshots; reconciliation of accepted/processing/unknown stages; exact-first mapping and Package A output.

Retained source count 108 and old provisional 65/108 missing outputs are historical/non-authoritative. V1/V2/V3/V4 are retired.

### #119 — Legendary Poet Shorts/Clips reconciliation

Exact binding: `legendary-poet`, alias `legendary-poet`, YouTube `UC-78ys2S3cQ3lpqgXfo-SvQ`, community/owner `235216998` / `-235216998`.

Required: exact V3/V4 plan/result/journal/ledger evidence; fresh YouTube Shorts and VK ordinary/Clip snapshots; Package A evidence; classification of native Clip, ordinary copy, duplicate, proof-backed missing, unknown, and repair-only. Old ordinary VK Videos remain untouched.

Retained `56 / 41 / 15 / 0`, `BXZeRiEOHmQ → -235216998_456239039`, and “48 clips” are inputs/history, not fresh truth.

### #38 — shared provider-mode/final-type contract

Project-neutral; no queue ownership. Requires dated current primary sources, versioned `external_embed` / `native_video` / `native_clip` modes, exact adapter evidence, one canary after provider/policy changes, processing-complete final type readback, and unknown-outcome reconciliation.

Conflicting historical duration claims are not a contract. Duration, geometry, title, player appearance, or ordinary `video.get` absence never proves native Clip identity.

### #33 — later Lord God video catalog/publication gate

Blocked by #31 and #32. Any later plan must be immutable, exact-ID, project-bound, and postflight-verifiable. VK Audio/MP3 and Legendary Poet are excluded.

### #99 — separate Legendary Poet article-wall workflow

Bound to community `235216998` and separate from #119. Requires supported adapter readiness, published+postponed wall preflight, exact assets/text/schedule, one canary, durable results, and postflight.

### #37 — independent exact cleanup

Owns only its explicitly reviewed object set and authorizes no inferred bulk deletion.

## Permanent package and handoff rules

Every package declares one evidence level, runs repository-owned acceptance, binds exact project/OAuth/community/owner/entrypoint, keeps `provider_writes_authorized=false` and `automatic_execution=false`, records durable per-operation results, and stops on unknown outcomes.

Every Windows handoff follows `.github/copilot-instructions.md`: self-contained PowerShell, exact paths, `-LiteralPath`, `Test-Path`, explicit extraction, full-path invocation, `$PSScriptRoot`, exact-one artifact, no `LastWriteTime`/newest ZIP/undefined variables, declared truth/capability/project binding, and UTF-8/BOM for Russian `.ps1`/`.txt`.

## Historical learning and VK Audio

Preserve source-bound Markdown/JSON archives with SHA-256, line counts, evidence level, unavailable evidence, and rerun prohibition. Transcript/stdout success is not independently `batch_verified` without per-operation results and exact postflight. Keep v2 immutable and use versioned overlays.

VK Audio remains separate and unsupported until a reviewed adapter owns schemas, session acquisition, upload-ticket validation, exact identity, per-item ledger, canary, postflight, and reconciliation.

## Definition of done

A wave closes only after focused scope, fail-closed evidence, exact-head six-job green CI, completed state sync, issue/roadmap updates, and preserved provider safety boundaries.
